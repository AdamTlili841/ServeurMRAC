services:
  - type: web
    name: serveur-mrac
    runtime: docker
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: CORS_ORIGINS
        value: "*"
      - key: CHECKPOINT_PATH
        value: /app/best_model.pt
